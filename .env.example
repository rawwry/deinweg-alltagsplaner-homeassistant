# Server Port
PORT=4731

# JWT Secret Key
JWT_SECRET=dein-weg-alltagsplaner-secret-key-2026

# SQLite Database URL (defaults to ./prisma/dev.db in local development,
# and /share/deinweg-alltagsplaner/db/alltagsplaner.db on Home Assistant OS)
# DATABASE_URL="file:./prisma/dev.db"
